
while(True):
    L = list(map(int,input().split()))
    L.sort()
    a = L[0]
    b = L[1]
    c = L[2]
    if(a!=0 or b!=0 or c!=0):
        if (a**2 + b**2 == c**2):
            print("right")
        else:
            print("wrong")
    else:
        break
